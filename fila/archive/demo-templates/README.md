# Demo / template pages (archived)

These HTML files are **template demos** (CRM, LMS, HR, Projects, Email, UI kits, etc.) from the Fila theme. They are not part of the core Cloudignite restaurant app.

- **Core app** lives in `fila/core/`.
- Asset paths here point to `../../assets/`.
- Links to the main app use `../../core/…`.

See `docs/FILA_RESTAURANT_CORE_PLAN.md` and `docs/FILA_HTML_CATEGORIZATION.md`.

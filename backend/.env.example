# Cloudignite Cost Control — Backend
# Copy to .env and fill in values. Never commit .env.

# Required
JWT_SECRET=                            # Min 32 chars. Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Server
PORT=8000
NODE_ENV=development

# Database (optional; app uses ./database/restaurant_cost.db by default)
# DATABASE_PATH=./database/restaurant_cost.db

# Uploads
# UPLOAD_DIR=./uploads
# Max upload size handled in Express (e.g. 10mb).

# CORS (comma-separated; default allows localhost:3000, 8080)
# ALLOWED_ORIGINS=http://localhost:3000,https://your-app.com

# Square API (optional; used for sync)
# SQUARE_APPLICATION_ID=
# SQUARE_ACCESS_TOKEN=
# SQUARE_LOCATION_ID=
# SQUARE_ENVIRONMENT=sandbox

# Feature flags
# ENABLE_OWNER_VIEW=true   # Set to false to disable Owner View (executive-summary returns 503)

# Performance (cache speeds up dashboard/analytics)
# ENABLE_CACHE=true
# CACHE_TTL_DASHBOARD=300000
# CACHE_TTL_ANALYTICS=600000
# CACHE_MAX_SIZE=200
# ENABLE_QUERY_OPTIMIZATIONS=true

# OCR / external services (if used)
# OCR_SERVICE_URL=

# Logging
# LOG_LEVEL=info
# ENABLE_QUERY_LOGGING=false

# Maintenance
# MAINTENANCE_MODE=false
# MAINTENANCE_MESSAGE=System under maintenance
